# SPDX-FileCopyrightText: 2026 David Rabkin
# SPDX-License-Identifier: 0BSD
---
name: checkmake
'on':
  pull_request:
    paths: &paths
      - .github/workflows/checkmake.yml
      - Makefile
  push:
    branches: [master]
    paths: *paths
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
defaults:
  run:
    shell: sh
jobs:
  checkmake:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          persist-credentials: false
      - id: cache
        uses: actions/cache@v6.1.0
        with:
          path: ./checkmake
          key: checkmake-0.3.2-${{ runner.os }}
      - if: steps.cache.outputs.cache-hit != 'true'
        run: |
          srv=https://github.com/mrtazz/checkmake/releases/download
          sum=e2effb876913f3ee2caef0ba35f6202c5e8a3cd55a077d8d2b9ce2034257b6af
          ver=0.3.2
          curl \
            --fail \
            --location \
            --output ./checkmake \
            --retry 3 \
            --silent \
            --show-error \
            "$srv/v$ver/checkmake-v$ver.linux.amd64"
          printf '%s  %s\n' "$sum" ./checkmake | sha256sum --check --status
          chmod +x ./checkmake
      - run: ./checkmake ./Makefile
